name: Build Application

on: push

jobs:
  build:
    name: Build app
    runs-on: ubuntu-latest
    steps:
      - name: Print current directory
        run: pwd
      - name: List files in driectory
        run: ls

